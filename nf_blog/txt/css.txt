:root {
    --transition: all 300ms ease;

    --container-width-lg: 74%;
    --container-width-md: 88%;
    --form-width: 40%;

    --card-border-radius-1: 0.3rem;
    --card-border-radius-2: 0.5rem;
    --card-border-radius-3: 0.8rem;
    --card-border-radius-4: 2rem;
    --card-border-radius-5: 5rem;
}

/* ======= Config. Geral ======= */

* {
    margin: 0;
    padding: 0;
    outline: 0;
    border: 0;
    appearance: 0;
    list-style: none;
    text-decoration: none;
}

body {
    font-family: 'Montserrat', sans-serif;
    line-height: 1.6;
    color: gray; /* Cor das letras */
    overflow-x: hidden;
    background-color: rgb(255, 255, 255); /* Cor de fundo */
    font-size: 0.9rem;
}

.container {
    width: var(--container-width-lg);
    max-width: 1800px;
    margin-inline: auto;
}

section {
    margin-top: 3rem;
    width: 100vw;
}

h1, h2, h3, h4, h5 {
    color: #111111; /* Cor dos títulos */
    line-height: 1.3;
}

h1 {
    font-size: 3rem;
    margin-bottom: 1rem 0;
}

h2 {
    font-size: 1.7rem;
    margin-bottom: 1rem 0;
}

h3 {
    font-size: 1.1rem;
    margin: 0.8rem 0 0.5rem;
}

h4 {
    font-size: 1rem;
}

a {
    color: #E67E22; /* Cor dos links */
    transition: var(--transition);
}

img {
    display: block;
    width: 100%;
    object-fit: cover;
}

/* ======= Nav ======= */
nav {
    background-color: #1A1A1A; /* Cor de fundo da navegação */
    width: 100vw;
    height: 4.5rem;
    position: fixed;
    top: 0;
    z-index: 10;
    box-shadow: 0 1rem 1rem rgba(0, 0, 0, 0.2);
}

nav button {
    display: none;
}

.nav__container {
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.avatar {  /* Estilo do avatar na nav bar */
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    overflow: hidden;
    border: 0.3rem solid #E67E22; /* Cor da borda do avatar */
}

.nav__logo {
    font-weight: 600;
    font-size: 1.2rem;
}

.nav__items {
    display: flex;
    align-items: center;
    gap: 4rem;
}

.nav__profile {
    position: relative;
    cursor: pointer;
}

.nav__profile ul {
    position: absolute;
    top: 125%;
    right: 0;
    display: flex;
    flex-direction: column;
    box-shadow: 0 3rem 3rem rgba(0, 0, 0, 0.4);
    background: #1A1A1A; /* Cor de fundo do menu suspenso */
    visibility: hidden; /* Esconder a lista de navegação por padrão. */
    opacity: 0;
    transition: var(--transition);
}

/* Exibir a lista de navegação ao passar o cursor sobre o perfil de navegação. */
.nav__profile:hover > ul {
    visibility: visible;
    opacity: 1;
}


.nav__profile ul li a {
    padding: 1rem;
    background: #1A1A1A; /* Cor de fundo dos itens do menu suspenso */
    display: block;
    width: 100%;
}

.nav__profile ul li:last-child a {
    background: #D35400; /* Cor de fundo do último item do menu suspenso */
    color: #1A1A1A; /* Cor do texto do último item do menu suspenso */
}
/* ======= Fim Nav ======= */


/* ======== Category button (botões de categoria) ======== */
.category__buttons {
    background-color: #ECECEC;
    color: #E67E22;
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: var(--card-border-radius-2);
    font-size: 0.8rem;
    font-weight: 600;
    text-align: center;
    margin-top: 0.1rem;
    margin-bottom: 1rem;
}

.category__buttons:hover {
    background-color: #E67E22;
    color: #ECECEC;
    transition: var(--transition);
}
/* ======== Fim category button  ======== */

/* ======== General post ======== */
.post__thumbnail { /* ======== Borda da imagem ======== */
    border-radius: var(--card-border-radius-2) 0;
    margin-bottom: 1.6rem;
    border: 0.7rem solid #1d1d1d;
}

.post__thumbnail img:hover {
    filter: saturate(0);
    transition: filter 500ms ease;
}

.post__author {
    display: flex;
    gap: 1rem;
    margin-top: 1.2rem;
}

.post__author-avatar {
    width: 2.5rem;
    height: 2.5rem;
    border-radius: var(--card-border-radius-3);
    overflow: hidden;
}

/* ======== Fim general post ======== */

/* ======== Featured ======== */

.featured {
    margin-top: 8rem;
}

.featured__container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
}

.featured .post__thumbnail {
    height: fit-content;
}

/* ======== Posts  ======== */
.posts__container {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 5rem;
    margin-bottom: 5rem;
}

/* ======== Categorias btns ======== */
.category__buttons-container {
    padding: 4rem 0;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
}

.category__buttons-container {
    width: fit-content;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
}

/* ======== Footer ======== */
footer {
    background: #1A1A1A; /* Cor de fundo do rodapé */
    padding: 5rem 0 0;
    box-shadow: inset 0 1.5rem 1.5rem rgba(0, 0, 0, 0.2);
}

.footer__socials { /* ======== Posicao dos ícones ======== */
    margin-inline: auto;
    width: fit-content;
    margin-bottom: 5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1.2rem;
}

.footer__socials a {  /* ======== Conf. vis. dos icones ======== */
    font-size: 1.5rem; /* ======== Tamanho dos ícones ======== */
    background: #1A1A1A; /* Cor de fundo dos ícones = cor de fundo do blog */
    border-radius: 50%;
    width: 2.3rem;
    height: 2.3rem;
    display: flex;
    justify-content: center;
    align-items: center;
}

.footer__socials a:hover {
    background: #E67E22; /* Cor de fundo dos ícones ao passar o mouse */
    color: #1A1A1A; /* Cor dos ícones ao passar o mouse */
    transition: var(--transition);
}

.footer__container {
    display:grid;
    grid-template-columns: repeat(4, 1fr);
}

footer h4{
    color: #E67E22; /* Cor dos títulos do rodapé */
    margin-bottom: 0.6rem;
}

footer ul li{
    padding: 0.3rem 0;
}

footer ul a{
    opacity: 0.75;
}

footer ul a:hover{
    letter-spacing: 0.2rem;
    opacity: 1;
}

.footer__copyright {
    grid-column: 1 / -1;
    text-align: center;
    margin-top: 3rem;
    color: #E67E22;
    margin-bottom: 2rem;
}

/* ======== Media Queries (Medium Devices) ======== */

@media screen and (max-width: 1024px) {

    .container {
        width: var(--container-width-md);
    }

    section {
        margin-top: 2rem;
    }

    h2 {
        font-size: 1.6rem;
    }

    h3 {
        font-size: 1.2rem;
    }

    h5 {
        font-size: 0.9rem;
    }

    .featured__container {
        grid-template-columns: 1fr;
    }

    .posts__container {
        grid-template-columns: 1fr;
    }

    /* ======== Nav ======== */
    .nav__items {
        display: none;
    }

     /* ======== Btn da nav no mobile ======== */
    nav button {
        display: inline-block;
        background: transparent;
        font-size: 1.5rem;
        color: #E67E22;
        cursor: pointer;
    }

    nav button#close__nav-btn {
        display: none;
    }

    .nav__container {
        position: relative;
    }

    .nav__items {
    display: none; /* Esconde a lista de navegação por padrão. */
    position: absolute;
    top: 100%;
    right: 0;
    background: #1A1A1A;
    width: 12rem;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding: 1rem;
    gap: 0;
    box-shadow: 0 3rem 3rem rgba(0, 0, 0, 0.4);
   }

    .nav__items li {
        width: 100%;
        height: 2.5rem;
        display:flex;
        align-items:center;
        background: #1A1A1A;
        border-top: 1px solid #E67E22;
        animation: animationDropdown 1s 0s ease forwards; /* Nav animation */
        opacity: 0; /* Inicia com opacidade 0 para a animação */
        transform-origin: top; /* Define a origem da transformação para o topo para a animação de dropdown */
    }

    .nav__items li:nth-child(2) {
        animation-delay: 200ms; /* Atraso para o segundo item */
    }
    .nav__items li:nth-child(3) {
        animation-delay: 400ms; /* Atraso para o terceiro item */
    }
    .nav__items li:nth-child(4) {
        animation-delay: 600ms; /* Atraso para o quarto item */
    }
    .nav__items li:nth-child(5) {
        animation-delay: 800ms; /* Atraso para o quinto item */
    }

    /* Nav animation */
    @keyframes animationDropdown {
        0% {
            transform: rotateX(90deg);
        }
        100% {
            transform: rotateX(0deg);
            opacity: 1;
        }
    }

    /* Config do itens da lista dentro do btn da nav */
    .nav__items li a {
        justify-content: center; /* Centraliza */
        border-radius: 0;
        width: 100%;
        height: 100%;
        background: #1A1A1A;
        padding: 0 2rem;
        display: flex;
        align-items: center;
    }

    .nav__profile ul {
    display: none;
    }

/* Corrige o item do avatar no menu mobile */
    .nav__items .nav__profile {
        width: 100%;
        height: auto;        /* Remove a altura travada de 2.5rem do 'li' */
        display: flex;
        justify-content: center; /* Centraliza horizontalmente dentro dos 12rem */
        align-items: center;
        padding: 1rem 0;     /* Dá um espaçamento confortável acima e abaixo do avatar */
        background: transparent;
    }

    /* Remove estilos globais extras caso existam interferindo */
    .nav__profile {
        width: auto;
        height: auto;
        margin: 0;
    }

    /* Define o tamanho exato e comportamento do círculo do avatar */
    .nav__profile .avatar {
        width: 3.0rem;       /* Tamanho proporcional ideal para caber nos 12rem do menu */
        height: 3.0rem;      /* Mantém a proporção idêntica para não achatar */
        border-radius: 50%;
        overflow: hidden;
        border: 2px solid #E67E22; /* Mantém a borda laranja elegante */
        display: block;
    }

    /* Garante que a foto preencha o círculo sem distorcer */
    .nav__profile .avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    /* ======== Posts para tablets ======== */
    .featured__container {
    gap: 3rem;
    }

    .posts__container {
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    }

    /* ======== Footer para tablets ======== */
    .footer__container {
    grid-template-columns: 1fr 1fr;
    }
}

/* ======== Media Queries (Small Devices) ======== */
@media screen and (max-width: 600px) {
    section {
        margin-top: 2rem;
    }

    h1{
        font-size: 2rem;
    }

    .featured {
        margin-top: 6rem;
    }

    .featured__container {
        grid-template-columns: 1fr;
        gap:0;
    }

    .posts__container {
        grid-template-columns: 1fr;
    }

    .category__buttons-container {
        grid-template-columns: 1fr 1fr;
    }

    .footer__container {
        grid-template-columns: 1fr;
        text-align: center;
        align-items: center;
        gap: 2rem;
    }
}